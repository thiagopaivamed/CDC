@model IEnumerable<string>

@foreach (var link in Model)
{
    @Html.RouteLink(link, new { controller = "Produto", action = "List", categoria = link, page = 1 }, new { @class = "btn btn-block btn-default btn-lg" + (link == ViewBag.CategoriaSelecionada ? " btn-primary" : "") })
}